# Chainlit UI — use latest (2.11+) which requires uvicorn>=0.35
# Do NOT pin uvicorn separately — let chainlit control it
chainlit>=2.11.0

# LangChain stack — unpinned so pip can resolve freely
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langchain-google-genai>=2.0
langchain-text-splitters>=0.3

# Vector store
faiss-cpu

# Document loading (lightweight — no torch/unstructured/sentence-transformers)
pypdf>=5.0
python-docx>=1.1

# Web framework — let chainlit's uvicorn requirement win
fastapi>=0.110

# Utilities
python-dotenv>=1.0
pydantic>=2.0
aiohttp>=3.9
